from comp import ispl, sum_of_digits
from files.tools.numbers.simp import Simp
from files.tools.col import Col


if __name__ == '__main__':
    # s = Simp()
    # print(s.addNumbers(4, 34))
    # c = Col()
    # c.compress(["a,txt", "b.txt"])
    print(sum_of_digits(123))

